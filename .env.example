# Optional: absolute control plane URL (e.g. prod or CORS testing in dev).
# In dev, if unset: empty default URL → /api and /auth requests proxied by Vite to localhost:5555.
# VITE_RESHAPR_SERVER=http://localhost:5555

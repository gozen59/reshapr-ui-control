# Optionnel : URL absolue du control-plane (ex. prod ou test CORS en dev).
# En dev, si absent : URL vide par défaut → requêtes /api et /auth proxifiées par Vite vers localhost:5555.
# VITE_RESHAPR_SERVER=http://localhost:5555
